# Host-friendly nim config for quick local test runs
# Based on tests/nim.cfg but adjusted so tests can be compiled+run on the host

# Use src on the path so `import nebble/ffi` resolves
--path:"../src"

# Keep default OS/CPU (host) so we can run tests locally

# Do not include bare-metal flags like --compileOnly, --noMain, --mm:arc, etc.

# Developer note: use this config for quick `nim c --run --config:tests/test_nim_host.cfg <test>`
